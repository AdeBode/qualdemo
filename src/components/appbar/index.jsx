import React from "react";
import "./appbar.scss";

const AppBar = () => (
  <header className="app-bar">
    <div>
      <h1>Contacts</h1>
      <h2>Organize your connections</h2>
    </div>
  </header>
);

export default AppBar;
